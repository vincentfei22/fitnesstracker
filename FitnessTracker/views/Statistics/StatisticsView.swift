import SwiftUI

struct StatisticsView: View {
    @EnvironmentObject var trainingData: TrainingData
    
    var body: some View {
        NavigationView {
            Text("统计功能正在开发中...")
                .navigationTitle("统计")
        }
    }
}
